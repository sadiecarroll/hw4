<h1>Places</h1>

<% if @user %>
  <ul>
    <% @places.each do |place| %>
      <li><%= link_to place.name, place_path(place) %></li>
    <% end %>
  </ul>
  <%= link_to "New place", new_place_path %>
<% else %>
  <p>You must first <%= link_to "login", login_path %> to see Places.</p>
<% end %>